package software.kanunnikoff.urlopener.presentation.model

import androidx.navigation3.runtime.NavKey

/**
 * Top-level destinations shown in the bottom navigation bar.
 */
enum class UrlOpenerTab : NavKey {
    Home,
    Settings,
    About,
}
